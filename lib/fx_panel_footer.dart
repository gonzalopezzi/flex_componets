import 'package:polymer/polymer.dart';

/**
 * A Polymer fx-panel-footer element.
 */
@CustomTag('fx-panel-footer')

class FxPanelFooter extends PolymerElement {

  /// Constructor used to create instance of FxPanelFooter.
  FxPanelFooter.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of fx-panel-footer is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of fx-panel-footer is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// fx-panel-footer is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when fx-panel-footer has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
