import 'package:polymer/polymer.dart';

/**
 * A Polymer fx-hbox element.
 */
@CustomTag('fx-hbox')

class FxHbox extends PolymerElement {

  /// Constructor used to create instance of FxHbox.
  FxHbox.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of fx-hbox is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of fx-hbox is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// fx-hbox is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when fx-hbox has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
