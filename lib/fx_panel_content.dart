import 'package:polymer/polymer.dart';

/**
 * A Polymer fx-panel-content element.
 */
@CustomTag('fx-panel-content')

class FxPanelContent extends PolymerElement {

  /// Constructor used to create instance of FxPanelContent.
  FxPanelContent.created() : super.created() {
  }

  /*
   * Optional lifecycle methods - uncomment if needed.
   *

  /// Called when an instance of fx-panel-content is inserted into the DOM.
  attached() {
    super.attached();
  }

  /// Called when an instance of fx-panel-content is removed from the DOM.
  detached() {
    super.detached();
  }

  /// Called when an attribute (such as  a class) of an instance of
  /// fx-panel-content is added, changed, or removed.
  attributeChanged(String name, String oldValue, String newValue) {
  }

  /// Called when fx-panel-content has been fully prepared (Shadow DOM created,
  /// property observers set up, event listeners attached).
  ready() {
  }
   
  */
  
}
